---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.9.2
  creationTimestamp: null
  name: helmbundles.kude.kfirs.com
spec:
  group: kude.kfirs.com
  names:
    kind: HelmBundle
    listKind: HelmBundleList
    plural: helmbundles
    singular: helmbundle
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HelmBundle describes a bundle that installs a Helm chart into
          the cluster.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: HelmBundleSpec describes the desired state of a HelmBundle
              in the cluster. It provides the necessary information on the Helm chart
              to be installed in the cluster.
            properties:
              chart:
                type: string
              release:
                type: string
              repository:
                type: string
              values:
                type: string
              version:
                type: string
            type: object
          status:
            description: HelmBundleStatus defines the observed state of a HelmBundle.
            properties:
              chartStatus:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
