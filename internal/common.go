package internal

const kudeDomain = "kude.kfirs.com"
const kudeFinalizerName = kudeDomain + "/finalizer"
const gitRepositoryTrackedCondition = "gitrepositories." + kudeDomain + "/Tracked"
const gitRepositoryClonedCondition = "gitrepositories." + kudeDomain + "/Cloned"
